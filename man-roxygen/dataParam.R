#' @param data Character. A \code{semiFrame} object can store models on both parametric and map data. Choose
#' \itemize{
#'   \item{ "par" }{ for model on parameters. }
#'   \item{ "map" }{ for model on maps. }
#'   \item{ "last" }{ for last modified model. }
#' }
