#' @param mapBorder Numeric of length 4. Specifies map cropping (negative values) or extending (positive values). Elements correspond to the up, right, down and left direction (default: \code{NULL}).
